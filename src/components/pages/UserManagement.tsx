import { FC } from 'react';
import { HeaderLayout } from '../template/HeaderLayout';

export const UserManagement: FC = () => {
  return (
    <HeaderLayout>
      <h1>ユーザー管理ページです。</h1>
    </HeaderLayout>
  );
};
